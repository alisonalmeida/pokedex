import 'package:flutter/material.dart';

class PokemonType extends StatelessWidget {
  const PokemonType({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
